import styled from 'styled-components'

import { Card } from 'components/Card'

export const StyledCastAvatar = styled(Card)(
  {
    position: 'relative',
    height: 0,
    paddingTop: '126.81%',
    overflow: 'hidden'
  }
)

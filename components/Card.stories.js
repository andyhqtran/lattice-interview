import React from 'react'

import { Card } from 'components/Card'

export default {
  title: 'Core/Card',
  component: Card
}

export const example = () => {
  return (
    <Card height={450} width={300} />
  )
}
